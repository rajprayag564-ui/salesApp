/**
 * src/types/database.types.ts
 *
 * ⚠️  PLACEHOLDER — DO NOT USE IN PRODUCTION
 *
 * This file will be automatically replaced with fully-typed definitions
 * generated directly from your live Supabase schema.
 *
 * ── How to regenerate ──────────────────────────────────────────────────────
 *
 * 1. Install the Supabase CLI (if not already installed):
 *      npm install -g supabase
 *    or via Scoop (Windows):
 *      scoop install supabase
 *
 * 2. Log in to Supabase:
 *      npx supabase login
 *
 * 3. Generate types from your project (replace <your-project-ref> with the
 *    value from Supabase Dashboard → Settings → General → Reference ID):
 *      npx supabase gen types typescript \
 *        --project-id <your-project-ref> \
 *        --schema public \
 *        > src/types/database.types.ts
 *
 * 4. Commit the generated file — regenerate whenever schema.sql changes.
 *
 * ── Alternative (local Supabase) ───────────────────────────────────────────
 *    If you run Supabase locally via `npx supabase start`:
 *      npx supabase gen types typescript --local > src/types/database.types.ts
 *
 * ──────────────────────────────────────────────────────────────────────────
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
